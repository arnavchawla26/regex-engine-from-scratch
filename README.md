# regex-engine-from-scratch
A regex engine built from scratch: Thompson NFA construction/simulation (no backtracking, always linear time) plus an independent naive backtracking matcher for comparison/benchmarking, sharing one re-flavored API and a grep-like CLI. Pure Python, no re module in the matching path.
